# UAG ARC Raiders Hub — Copilot CLI

The repository root `AGENTS.md` is the canonical AI-worker instruction set and must be followed for every task.

Do not run `copilot init` or overwrite the existing instruction system unless Mike explicitly asks for that.

Before editing, inspect the current worktree status and current implementation. One AI worker must own one Git worktree; never work in a worktree that another agent is actively modifying.
